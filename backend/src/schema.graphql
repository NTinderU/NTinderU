type User {
	id: ID!
	username: String!
	password: String!
}

type Query {
	"單個帳號"
	user(username: String!): String
	"所有帳號"
	users: [String!]!
}

type Mutation {
	"新增帳號"
	createUser(data: MutationInput!): User
	"刪除帳號"
	deleteUser(data: MutationInput!): User
}

input MutationInput {
	username: String!
	password: String!
}
